# Waitfans 项目 AI 开发要求

本文件对所有在本目录及子目录工作的 AI、自动化代理和开发者生效。三个应用是相互独立的 Git 仓库：

- `waitfans-backend`
- `waitfans-client`
- `waitfans-admin`

父目录 `waitfans` 本身不是 Git 仓库。禁止把父目录文件作为功能的唯一权威实现；需要长期保留的实现必须同时位于对应的子仓库中并提交。

## 强制 Git 提交规则

只要本次任务创建、修改、移动或删除了任何文件（包括源码、README、配置、脚本和测试），完成任务前必须执行以下流程：

1. 在修改前检查目标仓库的 `git status --short`，识别用户已有的未提交改动。
2. 只修改当前任务范围内的文件，不覆盖、回滚或顺带整理无关改动。
3. 完成实现后运行与风险相匹配的构建、测试、静态检查或功能验证。
4. 使用 `git diff --check` 和 `git diff` 复核最终差异。
5. 只暂存本次任务产生的文件；不得使用会误收无关改动的宽泛暂存方式。
6. 在每一个受影响的 Git 仓库中分别创建提交。不得把多个独立仓库当成一次提交。
7. 提交后再次运行 `git status --short`，确认本次任务没有遗留未提交文件。
8. 最终回复必须列出每个新提交的仓库、短哈希、提交标题和验证结果。

任何文档、配置或“一行小改动”都不能跳过提交。没有文件变化时不要创建空提交。

## 提交范围与安全

- 只提交当前 AI 在当前任务中创建的改动。
- 如果工作区已有用户改动，必须保留它们；能够精确分离时只暂存本次文件，不能安全分离时先向用户说明阻塞。
- 不得提交 `.env.local`、密钥、密码、令牌、运行日志、PID、数据库文件、构建产物或 `.runtime` 内容。
- 使用清晰的 Conventional Commit 风格标题，例如 `feat: ...`、`fix: ...`、`docs: ...`、`chore: ...`。
- 除非用户明确要求，不得执行 `git push`、创建远程分支或 Pull Request。
- 如果测试失败、Git 身份缺失、提交钩子失败或提交无法安全完成，不得声称任务已完成；必须报告准确阻塞原因。

## 项目验证基线

- 后端改动：至少运行相关测试或 Maven 构建，并验证必要的服务/API。
- 用户端或管理端改动：至少运行 `npm run build`；涉及交互时还要进行浏览器验证。
- PowerShell 脚本改动：必须通过 PowerShell AST 语法解析，并实际执行安全的主路径或幂等路径。
- 纯文档改动：必须检查命令、相对链接和 `git diff --check`。

上述要求是任务完成条件，不是可选建议。
